import React from 'react'
import ReactDOM from 'react-dom'
import ReactTransitionCollapse from 'react-transition-collapse'

const Index = () => {
  return <ReactTransitionCollapse />
}

ReactDOM.render(<Index />, document.getElementById('root'))
